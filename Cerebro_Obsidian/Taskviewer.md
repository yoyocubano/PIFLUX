---
obsidianUIMode: preview
---
## <span style="color:rgb(255, 0, 0)">TODO</span>
```tasks
not done
hide backlink
filter by function task.status.type === "TODO"
group by function (task.heading + '.md' === task.file.filename) ? '' : task.heading

```


## <span style="color:rgb(255,165,0)">Limbo</span>
```tasks
not done
hide backlink
filter by function task.status.type === "IN_PROGRESS"
group by function (task.heading + '.md' === task.file.filename) ? '' : task.heading

```
